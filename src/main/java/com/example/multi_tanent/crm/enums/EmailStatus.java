package com.example.multi_tanent.crm.enums;

/**
 * Represents the status of an email sent through the CRM.
 */
public enum EmailStatus {
    DRAFT,
    SENT,
    FAILED,
    RECEIVED // For incoming emails
}